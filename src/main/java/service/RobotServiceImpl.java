package service;

import dto.RobotDTO;

public class RobotServiceImpl implements RobotService {

    @Override
    public RobotDTO getRobotProperties(Integer robotId) {
        RobotDTO result = new RobotDTO();
        return result;
    }
}
